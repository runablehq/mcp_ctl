{
  "name": "mcp-monorepo",
  "private": true,
  "workspaces": [
    "packages/mcpctl",
    "packages/registry"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  }
}
